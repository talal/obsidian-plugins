[workspace]
members = ["crates/formatter-cli", "crates/formatter-core", "crates/formatter-wasm"]
resolver = "2"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
