{
	"name": "formatter",
	"version": "0.1.0",
	"description": "Formatter plugin for Obsidian",
	"keywords": [],
	"license": "MIT",
	"type": "module",
	"main": "dist/main.js",
	"scripts": {
		"build": "npm run build:wasm && vp build",
		"build:wasm": "cd crates/formatter-wasm && wasm-pack build --target web --release && sed -i \"s/new URL('formatter_wasm_bg.wasm', import.meta.url)/'formatter_wasm_bg.wasm'/g\" pkg/formatter_wasm.js",
		"lint": "vp check",
		"lint:fix": "vp check --fix",
		"test": "vp test"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"obsidian": "latest",
		"typescript": "^7.0.2",
		"vite-plus": "latest"
	}
}
