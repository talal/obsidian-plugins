{
	"name": "typst-math",
	"version": "0.1.0",
	"description": "Render Typst math via MathML in Obsidian",
	"keywords": [],
	"license": "MIT",
	"type": "module",
	"main": "dist/main.js",
	"scripts": {
		"build": "npm run build:wasm && vp build",
		"build:wasm": "cd crates/typst-math-wasm && wasm-pack build --target web --release && sed -i \"s/new URL('typst_math_wasm_bg.wasm', import.meta.url)/'typst_math_wasm_bg.wasm'/g\" pkg/typst_math_wasm.js",
		"lint": "vp check",
		"lint:fix": "vp check --fix",
		"test": "vp test"
	},
	"devDependencies": {
		"@types/node": "^26.1.1",
		"obsidian": "latest",
		"typescript": "^7.0.2",
		"vite-plus": "latest"
	},
	"allowScripts": {
		"esbuild@0.25.5": true
	}
}
